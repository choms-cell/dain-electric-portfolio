배포 전 최종 체크리스트를 실행해주세요.

## 배포 전 체크리스트

### 🔴 필수 (배포 차단)

#### 빌드
- [ ] `npm run build` 에러 없이 성공하는가?
- [ ] TypeScript 타입 에러 없는가?
- [ ] ESLint 에러 없는가? (경고는 허용)

#### 환경변수 (Vercel 대시보드 확인)
- [ ] `NEXT_PUBLIC_SITE_URL` — 실제 배포 URL (예: https://dain-elec.co.kr)
- [ ] `NEXT_PUBLIC_SANITY_PROJECT_ID` — Sanity 연동 시 필수

#### 보안
- [ ] `.env.local`이 `.gitignore`에 포함되어 있는가?
- [ ] API 토큰이 클라이언트 코드에 노출되지 않는가? (`NEXT_PUBLIC_` 없는 변수)

---

### 🟡 권장 (배포 후 확인)

#### 실제 정보 입력 여부
- [ ] 전화번호 `041-000-0000` → 실제 번호로 교체했는가?
  - `src/app/page.tsx` CTA 섹션
  - `src/components/FloatingCTA.tsx`
  - `src/app/contact/page.tsx`
  - `src/app/layout.tsx` JSON-LD
- [ ] Footer 사업자번호, 대표자명 → 실제 정보인가?
- [ ] 이메일 `info@dain-elec.co.kr` → 실제 이메일인가?
- [ ] 주소 → 실제 주소인가?

#### SEO
- [ ] Google Search Console에 사이트맵 제출했는가?
- [ ] 대표 도메인으로 canonical URL이 설정되어 있는가?

#### 포트폴리오 콘텐츠
- [ ] Sanity에 실제 포트폴리오가 최소 1건 이상 있는가?
- [ ] 각 포트폴리오에 커버 이미지가 있는가?

---

### 🟢 성능 (선택)

- [ ] Lighthouse Performance 점수 80점 이상인가?
- [ ] 모바일 화면에서 FloatingCTA 버튼 노출 확인
- [ ] 포트폴리오 상세 페이지 Before/After 슬라이더 동작 확인

---

## 빌드 + push 실행

문제가 없으면 아래 명령어를 실행하세요:

```bash
source /Users/cho/.nvm/nvm.sh
cd "/Users/cho/Desktop/전기공사사이트"
npm run build
git add -A
git commit -m "feat: ..."
git push origin main
```

Vercel 자동 배포가 1~2분 내로 완료됩니다.
